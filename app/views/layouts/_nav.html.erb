<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="<%= root_url %>">Uprawnienia</a>
      <div class="container nav-collapse">
        <ul class="nav pull-right">
          <li><%= link_to "Pomoc", pomoc_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <% if user_signed_in? %>
                <%= current_user %>
              <% else %>
                Dostęp do portalu
              <% end %>
            <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <% if user_signed_in? %>
                <li>
                  <%= link_to "Twoje wnioski", orders_path %>
                </li>
                <li>
                  <%= link_to "Profil", edit_user_registration_path %>
                </li>
                <% if current_user.admin? %>
                  <li><%= link_to "Uprawnienia", admin_roles_path %></li>
                  <li><%= link_to "Oddziały", branches_path %></li>
                  <li class="divider"></li>
                <% end %>
                <li>
                  <%= link_to "Wyloguj", destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' } %>  
                </li>
              <% else %>  
                <li>
                  <%= link_to "Zaloguj", user_session_path %>
                </li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Zarejestruj", new_user_registration_path %>
                </li>
              <% end %>                                                            
            </ul>  
          </li>    
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>